version: '3.4'

services:
  api:
    image: ${DOCKER_REGISTRY-}sharpsonic
    volumes:
    - /host_mnt/d/Users/Ben/Documents/Audiobooks:/books
    - ./src/Api/appsettings.Development.json:/config/appsettings.json
    ports:
    - "32772:443"
    - "32773:80"
    build:
      context: .
      dockerfile: Api/Dockerfile